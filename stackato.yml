name: pinry
instances: 1
framework:
  type: python
mem: 128
hooks:
  post-staging:
    - python manage.py syncdb --noinput
    - python manage.py migrate --noinput
services:
  ${name}-db: mysql
ignores: ["dev.db", ".git"]
